defmodule GroupMessageTest do
  use ExUnit.Case
  doctest GroupMessage

  test "a person can sign up and get their own node" do
    # need to a test where it spawns a new node that can spawn messages
  end

  test "a person can sign out" do
    
  end

  test "a person can log in" do
    
  end

  test "a person can log out" do
    
  end

  test "a person can associate data" do
    
  end
end
